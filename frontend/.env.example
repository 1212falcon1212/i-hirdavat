# Backend API base URL (server + client)
NEXT_PUBLIC_API_URL=http://localhost:8002/api

# CMS sayfa kaydedildiginde Laravel `PageObserver`,
# `/api/revalidate` route'una bu gizli anahtarla istek atar.
# Backend `.env`'deki REVALIDATE_SECRET ile birebir AYNI olmalidir.
REVALIDATE_SECRET=
